# ── Core TerraMind stack (version-sensitive — do NOT change these) ──
terratorch>=1.2.4
diffusers==0.30.0
setuptools<81

# ── Deep learning ──
torch>=2.0.0
torchvision>=0.15.0

# ── Geospatial / data ──
rasterio>=1.3.0
numpy>=1.24.0
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0

# ── Evaluation ──
scikit-learn>=1.3.0
scipy>=1.11.0

# ── Visualisation / UI ──
streamlit>=1.32.0
folium>=0.16.0
streamlit-folium>=0.20.0
matplotlib>=3.8.0
Pillow>=10.0.0

# ── Training utilities ──
wandb>=0.17.0
pytorch-lightning>=2.0.0

# ── Satellite data access ──
sentinelhub>=3.9.0

# ── Orbital feasibility ──
onnxruntime>=1.18.0

# ── Notebook / misc ──
jupyter
gdown
tensorboard
tqdm